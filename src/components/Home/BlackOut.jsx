import React, { useRef } from "react";
import { motion } from "framer-motion";

const BlackOut = () => {
  return (
    <div className="flex flex-col justify-center items-center">
      <motion.div className="h-[300vh] w-full blah"></motion.div>
    </div>
  );
};

export default BlackOut;
